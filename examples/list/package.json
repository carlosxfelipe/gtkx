{
    "name": "list-example",
    "version": "0.0.0",
    "private": true,
    "description": "List example - Comprehensive showcase of ListView, GridView, and ColumnView",
    "type": "module",
    "scripts": {
        "build": "tsc -b",
        "start": "node dist/index.js",
        "test": "if [ -n \"$CI\" ]; then vitest run; else GDK_BACKEND=x11 xvfb-run -a vitest run; fi"
    },
    "dependencies": {
        "@gtkx/ffi": "workspace:*",
        "@gtkx/react": "workspace:*",
        "react": "19.2.1"
    },
    "devDependencies": {
        "@gtkx/testing": "workspace:*",
        "@types/react": "19.2.7",
        "vitest": "4.0.15"
    }
}
