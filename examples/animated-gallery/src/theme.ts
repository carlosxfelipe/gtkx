import { css } from "@gtkx/css";

export const arenaStyle = css`
    background-color: alpha(currentColor, 0.06);
    border-radius: 16px;
`;

export const trackStyle = css`
    background-color: alpha(currentColor, 0.06);
    border-radius: 12px;
    min-height: 44px;
`;

export const chipStyle = css`
    background-color: alpha(currentColor, 0.1);
    border-radius: 6px;
    padding: 2px 8px;
    font-family: monospace;
    font-size: 0.85em;
`;

export const monoStyle = css`
    font-family: monospace;
`;

export const handleRowStyle = css`
    padding: 8px;
`;
