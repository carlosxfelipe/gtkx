import { defineConfig } from "vitest/config";

export default defineConfig({
    test: {
        include: ["tests/**/*.test.{ts,tsx}"],
        globals: false,
        bail: 1,
        pool: "forks",
    },
    esbuild: {
        jsx: "automatic",
    },
});
