{
    "name": "<%= name %>",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "gtkx dev src/dev.tsx",
        "build": "tsc -b",
        "start": "node dist/index.js"<% if (testing === 'vitest') { %>,
        "test": "vitest"<% } %>
    },
    "gtkx": {
        "appId": "<%= appId %>"
    }
}
